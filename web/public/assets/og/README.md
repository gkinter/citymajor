# Open Graph image assets

Place the default social preview image at:

```
web/public/assets/og/citymajor-og.png
```

Public URL (via `metadataBase`): `/assets/og/citymajor-og.png`

## Spec

| Property | Value |
|----------|-------|
| Filename | `citymajor-og.png` |
| Recommended size | 1200 × 630 px |
| Format | PNG (or JPG) |
| Used by | Root `app/layout.tsx` and `app/play/layout.tsx` `openGraph.images` |

## Status

**Placeholder path only** — the PNG is not committed yet. Add `citymajor-og.png` here before shipping social previews to production.
